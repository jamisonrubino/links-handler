<%= form_tag("/edit_links", method: "post", class: "links_index_form") %>

   <%= label_tag(:links, "Paste links URLs and sections (same as email format)") %><br>
   <%= text_area_tag(:links, "", size: "90x40") %><br>
   <%= submit_tag "Submit links" %>
